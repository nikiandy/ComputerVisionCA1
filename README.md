# o-ring inspection

this is my assignment 1 project
it checks all images in the `images` folder and marks each o-ring as pass or fail
it also writes the result image to `output`

install:
`pip install -r requirements.txt`

run:
`python main.py`
